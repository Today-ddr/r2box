{
  "name": "r2box-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "version:sync": "node -e \"const v = require('../version.json').version; const pkg = require('./package.json'); pkg.version = v; require('fs').writeFileSync('./package.json', JSON.stringify(pkg, null, 2) + '\\n');\""
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "pinia": "^2.1.7",
    "naive-ui": "^2.38.0",
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
